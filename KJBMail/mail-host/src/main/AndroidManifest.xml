<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:name="net.thunderbird.android.ThunderbirdApp"
        android:allowBackup="false"
        android:hasFragileUserData="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Thunderbird.Startup">
        <activity
            android:name="net.thunderbird.android.MailEntryActivity"
            android:exported="false"
            android:theme="@style/Theme.Thunderbird.Startup" />
        <activity
            android:name="com.fsck.k9.ui.settings.account.OpenPgpAppSelectDialog"
            android:configChanges="locale"
            android:theme="@style/Theme.Thunderbird.DayNight.Dialog.Translucent" />
        <activity
            android:name="com.fsck.k9.ui.notification.DeleteConfirmationActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/Theme.Thunderbird.DayNight.Dialog.Translucent" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <!-- We're using on-demand initialization for WorkManager. Leaving the default initializer in place
                 makes WorkManager start with a stock Configuration, so the app's WorkerFactory never applies and
                 every DI-constructed Worker (mail sync, account removal) fails silently. -->
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />

        </provider>

        <!-- This component is disabled by default (if possible). It will be enabled programmatically if necessary. -->
        <!-- IMPORTANT: The component name must be `net.thunderbird.android.widget.provider.MessageListWidgetProvider`
                        and can't be changed. It is referenced by name in `appConfig.componentsToDisable`; without this
                        declaration `Core.setServicesEnabled()` throws and the app crashes on startup. -->
        <receiver
            android:name="net.thunderbird.android.widget.provider.MessageListWidgetProvider"
            android:icon="@drawable/message_list_widget_preview"
            android:label="@string/message_list_widget_label"
            android:enabled="@bool/home_screen_widgets_enabled"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/message_list_widget_info" />
        </receiver>

        <!-- This component is disabled by default (if possible). It will be enabled programmatically if necessary. -->
        <!-- IMPORTANT: The component name must be `net.thunderbird.android.widget.provider.UnreadWidgetProvider`
                        and can't be changed. See the note above. -->
        <receiver
            android:name="net.thunderbird.android.widget.provider.UnreadWidgetProvider"
            android:label="@string/unread_widget_label"
            android:enabled="@bool/home_screen_widgets_enabled"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/unread_widget_info" />
        </receiver>
    </application>
</manifest>
